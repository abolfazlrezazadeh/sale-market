<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <div class="collapse navbar-collapse" id="collapsibleNavbar">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link <%= path==='/'  ? "active" : "" %> " href="/">خانه</a>
      </li>
      <li class="nav-item">
        <a class="nav-link <%= path=== '/main-products' ? 'active' : '' %> " href="/main-products"> محصولات</a>
      </li>
      <% if(isAuthenticated){ %>
      <li class="nav-item">
        <a class="nav-link <%= path==='/get-cart' ? 'active' : '' %> " href="/get-cart">سبد خرید</a>
      </li>
      <li class="nav-item">
        <a class="nav-link <%= path==='/orders' ? 'active' : '' %>" href="/orders"> سفارشات</a>
      </li>
      <li class="nav-item">
        <a class="nav-link <%= path==='/admin/add-product' ? 'active' : '' %> " href="/admin/add-product">افزودن محصول</a>
      </li>
      <li class="nav-item">
        <a class="nav-link   <%= path==='/admin/products' ? 'active' : '' %>" href="/admin/products"> محصولات ادمین</a>
      </li>
      <% } %>
      <!-- grouping -->
      <li class="nav-item">
        <div class="collapse navbar-collapse p-2" id="navbarSupportedContent">
          <form class="form-inline mr-auto" action="/grouping-products" method="post">
          <label for="title" style="color:white ;">دسته بندی</label>
          <select id="" name="grouping">
            <option value="گردنبند">گردنبند</option>
            <option value="گوشواره">گوشواره</option>
            <option value="گوشواره و گردنبند">گوشواره و گردنبند</option>
          </select>
          <button class="btn btn-rounded ml-sm-1 nav-link" type="submit"> انتخاب</button>
          </form>
        </div>
      </li>

      <!-- search box -->
      <li class="nav-item">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <form class="form-inline mr-auto" action="/search-products" method="post">
            <input value="<%= searchbox %>" name="search" class="form-control" type="text" placeholder="Search" aria-label="Search">
            <button class="btn btn-rounded ml-sm-2 nav-link  btn " type="submit">Search</button>
          </form>

        </div>
      </li>
    </ul>
  </div>
  <ul class="navbar-nav">
    <% if(!isAuthenticated){ %>
    <li class="nav-item">
      <!-- <input type="hidden" name="_csrf" value="<%= //csrfToken %>"> -->
      <a class="nav-link primary" href="/login">ورود</a>
    </li>
    <li class="nav-item">
      <!-- <input type="hidden" name="_csrf" value="<%= //csrfToken %>"> -->
      <a class="nav-link primary" href="/signup">ثبت نام</a>
    </li>
    <% } %>
    <% if(isAuthenticated){ %>
    <li class="nav-item">
      <!-- <input type="hidden" name="_csrf" value="<%= //csrfToken %>"> -->
      <a class="" href="/Edit-User" style="color:white ;margin-top: 10px;margin-left: 5px;"><%= userName %></a>
    </li>
    <li class="nav-item">
      <form action="/logout" method="post">
        <!-- <input type="hidden" name="_csrf" value="<%= //csrfToken %>"> -->
        <button type="submit" class="nav-link primary btn btn-link">خروج</button>
      </form>
    </li>
    <% } %>
  </ul>
</nav>